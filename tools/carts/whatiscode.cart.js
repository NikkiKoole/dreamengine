// whatiscode — a reading-heavy teaching cart, so it runs in a bigger canvas
// than the 320×200 game default: room for the 8px font on the code listing
// AND a proper "screen" box beside it. Primitives only — no sprites/map.
module.exports = {
  screenW: 480, screenH: 300, scale: 3,
}
