// PIXEL-PERFECT SCALING DEMO — primitives only (CPU pset_rgb), no sprites/map.
// 320×200 so three side-by-side viewports + labels fit; scale 3 for a roomy window.
module.exports = {
  screenW: 320,
  screenH: 200,
  scale: 3,
};
