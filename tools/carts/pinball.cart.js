// PINBALL runs portrait — a tall table. No sprites or map: the playfield is
// pure geometry (line-segment walls + circle bumpers), drawn with primitives.
module.exports = {
  screenW: 240,
  screenH: 320,
  scale:   2,
}
