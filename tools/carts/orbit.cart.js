// ORBIT — a gravity sandbox. Build a rocket, launch, and watch the predicted
// path curve into orbit (or, more often, into the ground). All primitives:
// planet, rocket, flame, debris and the dotted trajectory are drawn by hand.
// A wide canvas gives the orbit room to breathe.
module.exports = {
  screenW: 400,
  screenH: 250,
  scale:   3,
}
