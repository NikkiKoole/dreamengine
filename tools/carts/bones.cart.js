// BONES — runs on a roomier canvas so the 18×16 grid AND the live preview
// both fit side-by-side. No sprites/map: the whole tool is 1px line art.
module.exports = {
  screenW: 384,
  screenH: 240,
  scale: 3,
}
